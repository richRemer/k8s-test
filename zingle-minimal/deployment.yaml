apiVersion: apps/v1
kind: Deployment
metadata:
  name: zingle
  labels:
    app: zingle-minimal
spec:
  selector:
    matchLabels:
      app: zingle-minimal
  template:
    metadata:
      labels:
        app: zingle-minimal
    spec:
      containers:
      - name: zingle
        image: docker.io/library/php_php:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 9000
        volumeMounts:
        - name: zingle-cache
          mountPath: /opt/app-root/src/bootstrap/cache
      initContainers:
      - name: zingle-verify-host-entry
        image: docker.io/library/php_php:latest
        imagePullPolicy: Never
        command: ["sh", "-c", "cat /etc/hosts; echo; echo redis; getent hosts redis; echo zingle; getent hosts zingle"]
      volumes:
      - name: zingle-cache
        emptyDir: {}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  labels:
    app: zingle-minimal
spec:
  selector:
    matchLabels:
      app: zingle-minimal
  template:
    metadata:
      labels:
        app: zingle-minimal
    spec:
      containers:
      - name: redis
        image: redis:5.0.4
        command: ["redis-server", "/etc/redis/redis.conf"]
        ports:
        - containerPort: 6379
        volumeMounts:
        - mountPath: /var/lib/redis
          name: zingle-redis-data
        - mountPath: /etc/redis
          name: zingle-redis-conf
      initContainers:
      - name: redis-verify-host-entry
        image: docker.io/library/php_php:latest
        imagePullPolicy: Never
        command: ["sh", "-c", "echo; echo redis; getent hosts redis; echo zingle; getent hosts zingle"]
      volumes:
      - name: zingle-redis-data
        emptyDir: {}
      - name: zingle-redis-conf
        configMap:
          name: redis-conf
          items:
          - key: redis-conf
            path: redis.conf
